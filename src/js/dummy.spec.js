"use strict";

var expect = require("chai").use(require("sinon-chai")).expect;

describe("a dummy test", function () {
  it("should be working fine", function () {
    expect(2 + 4).to.equal(6);
  });
});